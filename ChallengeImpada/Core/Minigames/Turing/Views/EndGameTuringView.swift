import SwiftUI

struct EndGameTuringView: View {
    var body: some View {
        
        ZStack {
            
            Color.white
            
        }.frame(width: UIScreen.main.bounds.width * 0.45, height: UIScreen.main.bounds.height * 0.86)
    }
}
